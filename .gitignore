node_modules
.env
.DS_Store
**/.DS_Store

# Camera originals — keep locally, never commit
images/photos/originals/

# Backup archives — never commit these
Clone_*/
Backup_*/

# Full-res photo originals (source files, not served)
images/photos/originals/

# Mapbox style exports (reference only, not served)
**/Good_Style-copy*/

# Strava pipeline - inbox ZIPs and quality logs
pages/geo/runmap_v2/prepare_data/inbox/*.zip
data/json/strava/quality_report.txt

# Timeline raw export — keep locally, never commit (artifacts are rebuilt via build.py)
pages/geo/timeline_v3/prepare_data/source/
# Vercel local project metadata
.vercel
.env*
